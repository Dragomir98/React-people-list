const About = () => {
    return ( 
        <div className="about">
            <h3>This is a small project that gets data from a fake JSON server and that adding and deleting entries with data about different people.</h3>
        </div>
     );
}
 
export default About;